__pycache__/
*.pyc
*.egg-info/
.venv/
*.png
!examples/*.png
!charts/*.png
out.json

# scripts/build_site.py's build output -- published by .github/workflows/pages.yml,
# not committed.
_site/

# Copyrighted Sierra Club source documents — not redistributed (see DATA_LICENSE.md).
# Download from https://angeles.sierraclub.org/sierra_peaks to rebuild datasets.
data/source/*.pdf
data/source/*.xls
data/source/*.xlsx

# Rebuild-only staging file, superseded by the committed data/peaks.csv
# (core) + data/collections/sps.csv (SPS collection) split -- see
# scripts/split_collections.py and DATA_LICENSE.md's Source Policy section.
data/sps_peaks.csv
